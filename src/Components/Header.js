import {
    Card,
    Text,
    Display
  } from "@fluentui/react-components";



function Header() {
    return (
        <Card>
            <Text size={900} weight={`semibold`} color={`colorNeutralForeground1Hover`}>License Recommendation Tool</Text>
        </Card>
    );
  }
// export self made components always!
  export default Header;